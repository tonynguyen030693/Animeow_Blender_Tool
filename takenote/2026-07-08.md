# Take Note - 2026-07-08

Hôm nay đã hoàn thành việc tích hợp Studio Library API vào quy trình Publish File để tự động xuất dữ liệu chuyển động (Animation Export) chuẩn bị cho bước Gộp Cảnh (Merge/Combine):

## 1. Bản đồ thư mục Studio Library tối ưu
* Thư mục thư viện chính thức cho từng tập phim:
  `Z:/Animeow_Production/{Project}/{Episode}/Published/StudioLibrary/`
* Thư mục phân chia theo từng Shot:
  `.../Published/StudioLibrary/Shot_{shot_num}/`

## 2. Tích hợp Tự động Xuất Anim khi Publish
* Khi artist click chuột phải chọn **🚀 Publish File này (Copy nhanh)**:
  * Tool sẽ đối chiếu xem tệp được chọn có phải là scene đang mở trong Maya hay không.
  * Nếu khớp (đang làm trên scene hiện tại), tool thực hiện copy file Maya sạch lên server và chạy ngầm quy trình xuất chuyển động qua Studio Library API (`mutils.saveAnim`).
  * Nếu không khớp (publish offline tệp khác), tool hiển thị thông báo hướng dẫn: *"Bạn đang publish offline một tệp không phải scene đang mở hiện tại, vì vậy hệ thống không thể xuất dữ liệu anim..."*.

## 3. Cấu trúc xuất Anim linh hoạt
Khi xuất anim, tool tự động chia dữ liệu thành 2 phần:
* **all_assets.anim:** Lưu chuyển động của tất cả Rig có keyframe trong scene cùng lúc, phục vụ trực tiếp cho khâu Gộp Cảnh tự động 1-click.
* **{Namespace}.anim:** Các file anim tách lẻ theo namespace của từng Rig (nhân vật/vật thể) để artist có thể kéo thả sử dụng linh hoạt qua UI Studio Library khi cần.

## 4. Viết mới Animeow Maya Toolboard (Python 3) cho Maya 2022+
* Đã chuyển đổi và viết mới toàn bộ gói `animeow_maya_toolboard` đặt trong thư mục `Maya_script_2022_python_3`.
* Loại bỏ tính năng Ghép nối cũ (Anim Combiner).
* Tích hợp tính năng **Smart Link (Double Locator System)** hoạt động tương tự Blender:
  * **Link (Locator Pair):** Tạo cặp locator `parent` (Hook) và `child` (Offset) lồng nhau, tự động ghi chuyển động cũ thế giới để nạp sang locator con.
  * **Switch Target:** Chuyển đổi Driver dẫn đường sang đối tượng khác tại frame chỉ định mà không giật lệch vị trí trực quan.
  * **Bake & Clean:** Nướng lại chuyển động thực tế cho vật bị dẫn, gỡ constraint và tự động dọn sạch locator dư thừa.
  * **Smart Clean (Tùy chỉnh):** Giữ nguyên lưới Grid Step (ví dụ step=2: 1, 3, 5, 7...) kết hợp giữ nguyên chính xác các keyframe nguồn của Locator/Target nằm ngoài lưới (ví dụ: key cực trị ở frame 4) để artist dễ quản lý key mà không mất chuyển động chính.
* Tích hợp Menu, Shelf Button (thông qua `install.mel` kéo thả) tiện lợi cho artist.
  * **Tích hợp 4 Công cụ Tiện ích mới:** Thêm GroupBox khởi chạy nhanh cho **Studio Library, DWPicker, Tween Machine và aTools** trên giao diện Toolboard. Hệ thống sẽ tự động cấu hình nạp các đường dẫn Python & MEL cần thiết từ thư mục tham chiếu `scripts_2022` của dự án.

## 5. Hoàn thiện Chế độ Quay thử hàng loạt (Multi-Camera Playblast)
* Đã nâng cấp tab Playblast cho phép tích chọn chế độ **"Quay hàng loạt (Multi-Camera)"**:
  * Khi kích hoạt, hệ thống sẽ hiện danh sách checkbox để chọn các camera muốn quay cùng lúc.
  * Tích hợp **QProgressDialog** hiển thị tiến trình quay từng camera và cho phép bấm Hủy (Cancel) bất kỳ lúc nào.
  * Tự động tắt tính năng Auto-Play Viewer cho tất cả camera trừ camera cuối cùng trong danh sách để tránh mở quá nhiều cửa sổ VLC làm đơ hệ thống.
  * Xuất báo cáo kết quả chi tiết (đếm số camera thành công/thất bại và liệt kê lỗi nếu có).

## 6. Viết mới Arc Tracker siêu nhẹ (Motion Trail)
* Giải quyết triệt để lỗi lag viewport của Motion Trail mặc định trong Maya.
* **Thuật toán quét tọa độ chính xác cao:** Duyệt qua các khung hình tuần tự kết hợp đóng băng kết xuất màn hình (`cmds.refresh(suspend=True)`) giúp trích xuất tọa độ Rotate Pivot thế giới chính xác 100% dưới mọi cấu hình đánh giá (Parallel/DG), đồng thời không gây trễ lag viewport.
* Vẽ đường dẫn dưới dạng **Static NURBS Curve** giúp viewport mượt mà 100% khi playback hoặc scrub thời gian.
* Hiển thị điểm Ticks thường (màu vàng) và điểm Ticks Keyframe (màu đỏ rực rỡ, được phóng đại 2x) giúp dễ quan sát spacing/timing.
* **Tích hợp tính năng Cập nhật thủ công (Update Trails):**
  * **Nút bấm mới:** Thêm nút **Cập nhật Arc Trails (Update)** giúp artist nhanh chóng vẽ lại trail sau khi tinh chỉnh animation.
  * **Cơ chế cập nhật thông minh:** Nếu chọn vật thể, chỉ cập nhật trail của vật thể đó. Nếu không chọn gì, hệ thống tự động quét toàn bộ group tổng và cập nhật đồng loạt tất cả các trail đang hiển thị trong cảnh chỉ bằng 1 click.
  * **Gắn thẻ kết nối (Message connection):** Lưu liên kết của đối tượng gốc trực tiếp lên group trail của nó thông qua thuộc tính ẩn `animeow_sourceObj` để cập nhật chính xác bất kể thay đổi về namespace hay phân cấp cha.

## 7. Phát triển Smart World Bake (Tab 5)
* Tự đóng gói thư viện `ml_worldBake` và `ml_utilities` vào thư mục `thirdparty/` của Toolboard để chạy offline độc lập trên mọi máy.
* Phát triển riêng một tab **Smart World Bake** hoàn chỉnh của Animeow kế thừa thuật toán nướng thông minh (Smart Clean):
  * Artist có thể chọn kênh ràng buộc cụ thể khi polish: `Both` (Cả hai), `Translate Only` (Chỉ dịch chuyển), hoặc `Rotate Only` (Chỉ xoay).
  * Hỗ trợ nướng thưa theo lưới Grid Step (2s, 3s...) kết hợp tự động nhận diện và bảo toàn nguyên vẹn các Keyframe cực trị (Extreme Pose) ban đầu để đường cong animation không bị nát hay mất dáng.
  * Cơ chế kết nối Locator thế giới qua thuộc tính ẩn `animeow_bakeSource` giúp tự động nhận diện liên kết khi nướng ngược trở lại vật thể gốc và tự động dọn dẹp sạch locator rác.

## 8. Tích hợp cụm tiện ích nhanh hệ thống Maya
* Thêm nhóm công cụ **"Tiện ích nhanh Maya"** trong Tab 2 (Quick Tools) gồm:
  * **📈 Graph Editor:** Hỗ trợ Bật/Tắt (Toggle) nhanh cửa sổ Graph Editor. Nếu đang mở thì đóng để tiết kiệm diện tích màn hình, nếu chưa mở thì mở lên nhanh chóng.
  * **📂 Reference Editor:** Bật/Tắt nhanh cửa sổ Reference Editor của Maya giúp quản lý file reference nhanh gọn.
  * **💾 Save Increment:** Gọi tính năng lưu file phiên bản kế tiếp (`IncrementAndSave` của Maya) tạo hậu tố `.0001`, `.0002`...
  * **🚀 Save Up Version:** Tự động tăng version chính của file (ví dụ `_v01` -> `_v02`) và tự động reset các hậu tố increment phụ phía sau (lược bỏ phần `.0001` dôi dư). Có bảng xác nhận an toàn trước khi lưu.
  * **🧹 Clean Folder (Dọn dẹp thư mục):** Tự động lọc tất cả các phiên bản tệp liên quan trong thư mục scene hiện tại. Giữ lại **tối đa 5 phiên bản mới nhất** (bao gồm cả tệp đang mở trong Maya để tránh lỗi handle file), toàn bộ các phiên bản cũ hơn sẽ được tạo và di chuyển ngăn nắp vào thư mục con `Old/` của scene đó.
  * **🔄 Euler Filter:** Tích hợp bộ lọc euler nhanh ngay bên cạnh Graph Editor giúp animator khử xoay/lật khớp (gimbal flips) chỉ với 1 click. Tool tự động nhận diện nếu đang chọn đường cong trong Graph Editor thì lọc các đường cong đó, hoặc nếu chọn vật thể trong viewport thì lọc toàn bộ các kênh xoay của vật thể, hỗ trợ Ctrl+Z hoàn tác cực nhanh.
  * **🩹 Fix Lost Shader (Sửa lỗi mất shader):** Tự động mở khóa (unlock) hai node hệ thống mặc định của Maya là `initialShadingGroup` và `defaultTextureList1` giúp khắc phục triệt để lỗi mesh bị đổi màu xanh lá khi import/export hoặc gán chất liệu lỗi.

## 9. Phát triển Công cụ Làm tròn số (Round Tool)
* Thêm nhóm công cụ **"Làm tròn số (Round Values)"** trong Tab 2 (Quick Tools) hỗ trợ tối ưu hóa các giá trị số thập phân:
  * **Hỗ trợ 3 độ chính xác:** Làm tròn về số nguyên (ví dụ: 1), làm tròn đến 1 số thập phân (ví dụ: 1.1) hoặc 2 số thập phân (ví dụ: 1.23).
  * **Hỗ trợ 2 môi trường đích:**
    * **Channel Box:** Làm tròn trực tiếp giá trị của các thuộc tính (channels) được chọn trên vật thể trong Channel Box (nếu thuộc tính đang có keyframe ở frame hiện tại, keyframe đó cũng tự động được làm tròn theo).
    * **Graph Editor:** Làm tròn các Keyframe được chọn trên đường cong chuyển động. Nếu không chọn keyframe cụ thể, công cụ tự động làm tròn toàn bộ keyframe của các thuộc tính được chọn để làm mượt đường anim.
  * **Hỗ trợ hoàn tác (Undo - Ctrl+Z):** Toàn bộ thao tác làm tròn được bọc trong một Undo chunk duy nhất của Maya giúp khôi phục dữ liệu tức thì nếu cần.

## 10. Phát triển Bộ công cụ Space & Rotate Order (Bảo toàn Keyframe)
* Thêm nhóm công cụ **"Space & Rotate Order (Bảo toàn Key)"** trong Tab 2 (Quick Tools):
  * **Đổi Rotate Order (Tự động):** Cho phép thay đổi thứ tự xoay (Rotate Order) của vật thể (XYZ, YZX, ZXY...) chỉ với 1 click. Hệ thống tự động trích xuất và bảo toàn nguyên vẹn toàn bộ các vị trí keyframe ban đầu của vật thể, tự động tính toán lại các giá trị xoay tương ứng để giữ nguyên dáng pose thế giới.
  * **Bộ đôi Ghi & Khôi phục Space Thế giới (Thủ công):**
    * **📥 Ghi Space Thế giới (Record):** Tạo một locator thế giới tạm, tự động nướng và ghi nhận toàn bộ chuyển động của control thế giới tại các mốc keyframe gốc của nó.
    * **📤 Khôi phục Space (Restore):** Nướng ngược lại chuyển động thế giới từ locator lưu trữ vào control tại đúng các mốc keyframe cũ, sau đó dọn dẹp sạch sẽ locator.
    * *Ý nghĩa:* Cho phép animator thay đổi parent, đổi constraint, đổi không gian Space của rig tùy ý, sau đó khôi phục lại chuyển động thế giới chính xác tuyệt đối mà không sợ làm mất hay thay đổi số lượng/vị trí keyframe ban đầu.
  * **Tối ưu hóa Undo:** Cả hai thao tác đều hỗ trợ hoàn tác Ctrl+Z nhanh.

## 11. Ghi nhớ trạng thái và vị trí cửa sổ (Float/Docked)
* **Khắc phục lỗi nhảy cửa sổ:** 
  * Trước đây, mỗi lần gọi khởi chạy tool, hệ thống đều xóa `workspaceControl` cũ, khiến layout luôn bị reset và nhảy về docked góc phải màn hình.
  * **Giải pháp tối ưu:** 
    * Hàm `show_window()` được cập nhật để **không xóa container `workspaceControl`** của Maya.
    * Khi reload/chạy lại tool, hệ thống chỉ giải phóng widget Python cũ và tạo mới một widget Python sạch sẽ, sau đó chèn trực tiếp vào container `workspaceControl` cũ đang có sẵn.
    * Kích hoạt thuộc tính hiển thị `visible=True` để cứu cửa sổ lên trong trường hợp người dùng vừa bấm tắt 'X' làm ẩn control, đồng thời gọi `restore=True` và `select=True` để lấy lại vị trí cũ.
  * **Hiệu quả:** Toolboard duy trì chính xác 100% vị trí nổi (Float Window), kích thước, hoặc vị trí ghép (Docked) mà artist đã thiết lập trước đó, hỗ trợ reload cập nhật code lập trình cực kỳ mượt mà không gây mất giao diện.

## 12. Nâng cấp và Cải tiến khác (Bổ sung ngày 08/07)
* **Làm tròn số keyframe chi tiết (Detailed Keyframe Rounding)**:
  * Nâng cấp công cụ làm tròn số để hỗ trợ 4 chế độ làm tròn linh hoạt:
    1. **Channel Box (Thuộc tính chọn)**: Làm tròn các giá trị tĩnh hoặc keyframe hiện hành.
    2. **Graph Editor (Keyframe chọn)**: Chỉ làm tròn những keyframe được quét chọn thủ công trong Graph Editor.
    3. **Keyframe tại frame hiện tại**: Chỉ làm tròn keyframe ở đúng thời điểm thanh timeline đang đứng (tiện lợi, an toàn và đúng mốc timeline).
    4. **Toàn bộ keyframe (Timeline)**: Làm tròn tất cả các keyframe trên toàn timeline của các thuộc tính được chọn.
* **Smart Bake (Key-on-key) trong World Bake**:
  * Tích hợp tùy chọn **Smart Bake (Chỉ Bake tại các frame có Key của nguồn)** vào nhóm World Bake.
  * Khi nướng sang Locator hoặc ngược về Vật thể, tool chỉ tạo keyframe tại đúng các frame có key của đối tượng nguồn ban đầu giúp bảo toàn timing, tránh tạo key dày đặc ở các frame khác.
* **Chuẩn hóa thuật ngữ "Bake"**:
  * Chuẩn hóa từ dịch tiếng Việt "nướng" thành thuật ngữ chuyên ngành chuẩn là **"Bake"** trên toàn bộ giao diện, Popup cảnh báo/kết quả và log/comments hệ thống.
* **Sửa lỗi hiển thị cụt chữ trên thanh Tab (Tab Truncation & PySide2 Bug)**:
  * Khắc phục triệt để lỗi hiển thị bị cắt cụt chữ (ví dụ: `Link & Bake` bị cụt thành `Link & Bak`, `Playblast` thành `Playblas`,...) do bug đếm ký tự surrogate pair (emoji UTF-16) của PySide2/Shiboken.
  * **Giáp pháp:** Thêm khoảng trắng bù đắp cuối chuỗi kết hợp đặt `setElideMode(ElideNone)`, `setUsesScrollButtons(True)` và co gọn khoảng đệm tab trong QSS (`padding: 8px 8px`) giúp thanh tab luôn hiển thị đầy đủ tiêu đề đẹp mắt.
* **Bộ công cụ Rig Retargeting Tool (Mới)**:
  * Thiết kế và phát triển tab Retarget chuyên nghiệp hỗ trợ chuyển đổi chuyển động giữa 2 Rig bất kỳ:
    1. **Quản lý Namespace**: Tự động làm mới và quét danh sách các Namespace Rig hiện hữu trong cảnh.
    2. **Khớp tự động (Auto Map)**: Tự động ghép nối các bộ điều khiển có tên tương đồng giữa hai Namespace.
    3. **Gán thủ công từ Viewport**: Bổ sung hai nút **Lấy Nguồn (Get Source)** và **Lấy Đích (Get Target)** cho phép gán trực tiếp đối tượng chọn trong Viewport vào bảng ánh xạ. Rất thích hợp khi hai rig có hệ thống xương, tên gọi hoặc namespace khác biệt hoàn toàn (ví dụ: `RigA:Root` khớp với `RigB:cog`).
    4. **Lưu/Tải file JSON**: Hỗ trợ lưu trữ bảng ánh xạ cấu hình ra file JSON để tái sử dụng nhanh chóng.
    5. **Bảo toàn & Snap vị trí (Maintain Offset)**: Tích hợp checkbox Maintain Offset. Bỏ tích chọn để **snap khớp hoàn toàn vị trí và hướng xoay thế giới (World Space)** của Rig đích theo Rig nguồn, vượt qua mọi sự khác biệt về phân cấp cha con (hierarchy) của Rig.
    6. **Smart Bake**: Chỉ nướng chuyển động tại đúng các frame có keyframe của Rig nguồn để giữ timing sạch.
* **Bộ công cụ Mirror Animation Tool (Mới)**:
  * Thiết kế và phát triển tab Mirror Anim hỗ trợ 4 chế độ lật chuyển động: Swap Left & Right, Left -> Right, Right -> Left, và Flip Selected.
  * Tự động nhận diện trục đối xứng cho các control bên (L/R) dựa trên tiền tố/hậu tố.
  * Các control chính giữa (Center) như Hip, Spine, Head tự động lật chính nó (Flip) trên các trục đã cấu hình.
  * Sử dụng các lệnh gốc của Maya (`copyKey`, `pasteKey`, `scaleKey`) để tối ưu hóa hiệu suất và bảo toàn 100% độ cong tiếp tuyến (tangent handles).
* **Bộ công cụ Temp Pivot Tool (Tâm xoay tạm thời) (Mới)**:
  * Tích hợp cụm công cụ Temp Pivot vào Tab Quick Utilities.
  * Hỗ trợ tạo locator tâm xoay chung khi chọn **một hoặc nhiều** control (tự động tính tâm trung bình cộng).
  * Sử dụng cơ chế **Helper-based** đột phá để lưu trữ hoạt hình gốc của các control vào locator con, loại bỏ hoàn toàn lỗi hoạt hình/Double Transform khi kích hoạt.
  * Tự động dọn dẹp sạch sẽ các constraints/locator rác khi giải phóng.

