# Nhat ky cap nhat - 2026-07-22

## Animeow Enjo Pipeline - Fix loi trung shot va nang cap quy chuan ten file

### 1. Khac phục loi trung lap hau to Task va lặp Shot (_Anim_Anim)
- **Hien tuong**: Khi chon mot item Shot (vi du `LL_BGOTL_V01_Shot_01_Anim`) roi bam luu, ten file tao ra bi lặp hai lan `_Anim_Anim_v01.ma`. Danh sach Shot ben trai tu dong tao them mot Shot gia `LL_BGOTL_V01_Shot_01_Anim`.
- **Nguyen nhan**: Prefix shot khong duoc lam sach hau to `_Anim`/`_Lay` truoc khi noi them ten khau. Regex `SCENE_NAME_PATTERN` khong phan tich duoc cac file bi dinh them ky tu `_1`, `_2` o cuoi `_v01_1.ma`.
- **Giai phap**:
  - Them ham `clean_shot_code` trong `FileManager` de tu dong gọt sach `Shot_`, `_Anim`, `_Lay` va tien to tap phim tu chuoi nhap hoac item duoc chon.
  - Cap nhat regex `SCENE_NAME_PATTERN` ho tro `(?:_\d+)?` va them Fallback trong `parse_scene_name` de gom tat ca cac file `_v01_1.ma`, `_v01_2.ma`, `_Anim_Anim` về chung duy nhat Shot goc (vi du `LL_BGOTL_V01_Shot_01`).

### 2. Nang cap cong cu chuan hoa ten file hang loat
- Sửa hàm `rename_work_files`: Khi phat hien file nhat dinh da ton tai, thay vi them `_1`, `_2` (tao ra `_v01_1.ma`), he thong tu dong tang phien ban Version chuan: `_v02.ma`, `_v03.ma`, `_v04.ma`.
- Cap nhat `check_episode_filenames_naming` tu dong de xuat danh sach doi ten sach se va xep thu tu version tang dan cho cac file loi.

### 3. Tinh nang UI & Nap reload trong Maya
- **Xoa file truc tiep**: Nhap chuot phai vao danh sach File Maya o cot 2 -> Chon `🗑️ Xoa file Maya nay (Delete)` de xoa vinh vien file trung/nham khoi disk ngay tren UI.
- **Phan biet file trung**: Hien thi nhan vi tri `[file]` / `[Anim]` neu co file trùng version nam o 2 thu muc khac nhau, kem theo Tooltip hien thi duong dan goc.
- **Triet de Reload trong Maya**: Them ham `force_reload()` trong `__init__.py` de xoa sach cache `sys.modules` cua Maya moi khi goi `Animeow_Enjo_Pipeline.show()`, chong tinh trang Maya nhat quyet dung code cu trong RAM.

### 4. Dong bo hoa codebase
- Cap nhat va dong bo 100% code giua 2 thu muc:
  - `E:\AI_Work\Blender_Maya_Script\Maya_script\Animeow_Enjo_Pipeline\`
  - `E:\AI_Work\Blender_Maya_Script\Maya_script_2020_python_2\Animeow_Enjo_Pipeline\`
