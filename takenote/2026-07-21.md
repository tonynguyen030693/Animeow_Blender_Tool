# Nhật ký phát triển Animeow Maya Toolboard - 21/07/2026

## Các cập nhật & Nâng cấp cho Smart World Bake

### 1. Tách riêng cửa sổ World Bake
- Tách nhóm tính năng **World Bake (Bake không gian thế giới)** ra một cửa sổ độc lập (khi chạy ở chế độ `standalone_tab="world_bake"`).
- Ẩn toàn bộ các GroupBox không liên quan trên Tab 1: *Bake theo bước (Bake on Ns)*, *Tâm xoay tạm thời (Temp Pivot)*, và *Space & Rotate Order*.
- Đổi tên cửa sổ hiển thị thành **Smart World Bake** để mang tính định danh rõ ràng.
- Khắc phục lỗi lưu cache kích thước của Maya (`workspaceControl` sticky height bug):
  - Đổi tên Workspace Control định danh thành `AnimeowSmartWorldBakeWorkspaceControl`.
  - Tự động xóa `windowPref` liên quan trước khi hiển thị.
  - Định nghĩa lại kích thước hình học chuẩn cho cửa sổ là **Width: 350px / Height: 285px** để giao diện hiển thị cực kỳ cân đối, ôm sát nội dung.

### 2. Thêm ô quản lý chọn vật thể (Target Objects Selection Box)
- Bổ sung ô nhập liệu `Vật thể chọn` (`self.wb_objects_txt`) và nút **`Lấy chọn`** (`self.wb_get_objects_btn`) vào phần World Bake.
- Hệ thống tự động lấy danh sách đối tượng chọn khi khởi động nếu ô trống.
- Cập nhật hàm Bake sang Locator (`Record`) và Bake ngược về vật thể (`Restore`) để thực hiện chính xác trên các đối tượng được ghi trong ô chỉ định thay vì lấy trực tiếp từ selection hiện tại.

### 3. Cơ chế kiểm tra an toàn trạng thái Bake (Prevent Duplicate Bake)
- Thêm cơ chế kiểm tra xem vật thể đã có locator thế giới tương ứng hay chưa qua:
  - Thuộc tính kết nối tin nhắn ẩn (`.animeow_bakeSource`).
  - Kiểm tra theo tên locator chuẩn hóa (`Anm_loc_bake_<Tên_Vật_Thể>`).
- Khi bấm **Record**, hệ thống sẽ cảnh báo bằng hộp thoại và dừng xử lý nếu phát hiện đối tượng đã được bake trước đó, giúp giảm thiểu rác và lỗi keyframe chồng chéo trong Scene.

### 4. Nút bấm tạo Locator nhanh (Không Constraint)
- Thêm tính năng **Tạo Locator tại vị trí chọn (Không Constraint)** trực tiếp vào hàng nút của World Bake.
- Tự động đối chiếu hướng xoay thế giới, vị trí thế giới, và đồng bộ `rotateOrder` từ các vật thể nguồn sang locator mới để tránh lệch gimbal.

---

## Tính năng mới: Fake Constraint (Ràng buộc giả lập)

### 1. Ý tưởng & Mục tiêu:
* Giúp animator định vị và dịch chuyển nhanh một vật thể (Child/Follower) khớp theo chuyển động của vật chủ (Parent/Leader) mà không cần tạo parentConstraint thực tế của Maya. Tránh lỗi phân cấp và rác Scene.

### 2. Chi tiết triển khai:
* **Module tính toán lõi (`fake_constraint.py`)**:
  * Ghi nhớ ma trận khoảng cách tương đối (offset) tại khung hình mong muốn qua API v2:
    $$Offset = WorldMatrix_{child} \times WorldMatrix_{parent}^{-1}$$
  * Áp dụng offset lên khung hình đích:
    $$WorldMatrix_{child\_new} = Offset \times WorldMatrix_{parent\_new}$$
  * Tự động set keyframe cho các kênh di chuyển và xoay của đối tượng con.
  * **Hỗ trợ Nhiều vật theo (Multi-Follower)**: Lưu trữ các ma trận offset dưới dạng từ điển và mã hóa thành một chuỗi JSON lưu trong bộ nhớ `OptionVars` để áp dụng nhanh và chính xác cùng lúc cho nhiều đối tượng theo sau.
* **Giao diện độc lập (`standalone_tab="fake_constraint"`)**:
  * Gồm hai ô nhập dữ liệu: **Vật chủ (Leader/Parent)** và **Vật theo (Follower/Child)** cùng nút **Lấy chọn**. Ô *Vật theo* hiển thị danh sách cách nhau bởi dấu phẩy.
  * Dòng trạng thái báo khung hình đã ghi nhớ.
  * Các chức năng chính: **Ghi nhớ Offset**, **Match tới Frame hiện tại**, và **Bake Range** (dịch chuyển khớp cho cả một khoảng thời gian).
* **Đăng ký Shelf**:
  * Thêm nút **`FakeConst`** trên Animeow Shelf để mở nhanh công cụ.

---

## Loại bỏ công cụ: View Layer (Không còn sử dụng)
* Loại bỏ hoàn toàn công cụ **View Layer** theo yêu cầu của người dùng để làm gọn nhẹ bộ công cụ:
  * Xóa tệp nguồn `core/animeow_view_layer.py`.
  * Gỡ bỏ import và gỡ tab `View Layer` khỏi giao diện đầy đủ cũng như chế độ chạy độc lập trong `ui/window.py`.
  * Xóa nút bấm `ViewL` khỏi thanh công cụ `core/shelf.py`.

---

## Tích hợp công cụ mới lên Shelf: Curve Local Scale (L.Scale)
* Tích hợp công cụ **Curve Local Scale** (`mel/NP_curveLocalScale.mel`) lên thanh công cụ Shelf của Animeow:
  * Thêm nút bấm **`L.Scale`** trên Animeow Shelf.
  * Khi kích hoạt, hệ thống sẽ tự động tìm đường dẫn tuyệt đối của tệp MEL trong package và gọi lệnh `source` để hiển thị cửa sổ điều khiển tỷ lệ của Curve trong Maya.

---

## Sửa lỗi: Trùng lặp và Đổi tên Locator trong World Bake
* **Hiện tượng**: Khi dùng chức năng tạo locator không constraint hoặc bake sang locator, nếu trong scene đã có sẵn locator trùng tên (ví dụ đã bake từ trước và đang nằm trong group `Animeow_locator`), Maya tự động đổi tên locator mới tạo để tránh trùng lặp khi đưa vào group, khiến câu lệnh chọn (`cmds.select`) tiếp theo bị ném lỗi `No object matches name: |Anm_loc_match_pCube1`.
* **Khắc phục**:
  * Cập nhật `smart_link.parent_to_animeow_group` và `world_bake.parent_to_animeow_group` để trả về đường dẫn mới chính xác của node sau khi đưa vào group.
  * Tự động quét tìm và xóa sạch các locator cũ bị trùng tên trong scene bằng lệnh `cmds.ls(locator_name, long=True)` trước khi tạo mới, ngăn ngừa triệt để lỗi đổi tên tự động của Maya.

---

## Cải tiến UX: Hộp thoại xác nhận trùng lặp Locator (Yes/No/Cancel)
* **Yêu cầu**: Khi tiến hành Bake hoặc tạo Locator không constraint, nếu phát hiện locator tương ứng đã tồn tại từ trước trong scene, hệ thống cần đưa ra cảnh báo lựa chọn thay vì tự động xóa đi.
* **Triển khai**:
  * Khi phát hiện trùng lặp, hiển thị hộp thoại `Confirmation` với 3 tùy chọn:
    * **Yes**: Giữ lại các locator cũ, tự động tạo locator mới kèm theo hậu tố tăng dần (ví dụ: `_02`, `_03`, ...).
    * **No**: Dọn dẹp (xóa) locator cũ và ghi đè tạo lại locator mới.
    * **Cancel**: Hủy toàn bộ thao tác.
  * Tích hợp thành công cơ chế này vào cả chức năng **Bake sang Locator (Record)** và **Tạo Locator (Không Constraint)**.

---

## Tính năng mới: Tùy chọn Bake sang Locator không tạo Constraint
* **Mô tả**: Cho phép nướng chuyển động thế giới của vật thể sang locator mà không cần thiết lập mối ràng buộc ngược lại (`pointConstraint`/`orientConstraint`/`parentConstraint`) từ locator về vật thể gốc sau khi bake xong.
* **Triển khai**:
  * Thêm checkbox **Không Constraint** (`self.wb_no_constraint_cb`) ngay bên cạnh trường `Reducer Threshold` trong giao diện World Bake.
  * Tích hợp trạng thái checkbox này vào `load_settings` và `save_settings` với OptionVar `AnimeowTbWbNoConstraint` để duy trì lựa chọn của người dùng.
  * Cập nhật hàm `world_bake.bake_to_locator` chấp nhận đối số `no_constraint=False`. Nếu bằng `True`, bỏ qua bước tạo constraint ngược ở cuối quy trình, chỉ nướng chuyển động thuần túy lên locator mới.

---

## Quản lý Selection Sets: Tiền tố ANM_
* **Quy định**: Nhận diện các Selection Sets được tạo ra bởi bộ Animeow Toolboard thông qua tiền tố `ANM_` bắt buộc.
* **Cải tiến**:
  * Tự động kiểm tra và thêm tiền tố `ANM_` vào tên set khi người dùng bấm **Tạo Set Mới** hoặc **Đổi Tên** (nếu tên nhập vào chưa bắt đầu bằng `ANM_`).
  * Xử lý thông minh khi có namespace (ví dụ: `char:body` -> tự động đổi thành `char:ANM_body` thay vì đặt sai vị trí namespace).
  * Tích hợp cơ chế tự động gắn thẻ tiền tố này vào cả chức năng **Nhập Selection Sets từ File JSON** để đồng bộ hóa.
  * **Lọc bỏ Selection Sets rác**: Để tránh gây rối mắt do các selection sets đi kèm khi reference rig ngoài vào (Ví dụ: `lunoraNewFace_rig:FacialControls`, `lunoraNewFace_rig:Sets`), danh sách hiển thị trong bảng `Selection Sets Manager` giờ đây **chỉ nạp các sets thuộc về Animeow** (có tên chính bắt đầu bằng tiền tố `ANM_` hoặc được lồng đệ quy dưới group quản lý cha `Animeow_sets`).

---

## Sửa lỗi: Playblast xuất ra video không có âm thanh (Mất tiếng)
* **Hiện tượng**: Khi người dùng tắt tiếng hiển thị ở Time Slider hoặc tùy chọn hiển thị sound trên timeline bị ẩn/chuyển chế độ khác, hàm `cmds.timeControl(g_slider, sound=True)` trả về chuỗi rỗng `""`. Điều này làm công cụ bỏ qua không nạp node audio vào lệnh `cmds.playblast`, dẫn đến video đầu ra không có âm thanh dù scene có file nhạc.
* **Khắc phục**:
    * Tiễn biệt lỗiplayblast.get_active_sound trả về chuỗi rỗng: Quét node audio dự phòng trong scene, tự động tắt mute của audio node để đảm bảo audio luôn được ghi vào video playblast.

---

## Nâng cấp & Đồng bộ hóa Toolboard cho Python 2 (animeow_maya_toolboard_py2_v03)

Đã hoàn thành việc porting toàn bộ các nâng cấp từ phiên bản Python 3 (`v03`) sang nhánh Python 2 (`py2_v03`) tương thích với Maya 2020.

### 1. Đồng bộ và Tạo mới các tệp:
* **[fix_jitter.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/fix_jitter.py)**: Khử rung chuyển động.
* **[selection_sets.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/selection_sets.py)**: Quản lý Selection Sets phân cấp. Đã tùy biến thay thế `setSectionResizeMode` thành hàm fallback `setResizeMode` tương thích với phiên bản PySide2 đời cũ của Maya 2020.
* **[fake_constraint.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/fake_constraint.py)**: Giả lập ràng buộc.
* **[animeow_utilities.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/animeow_utilities.py)**: Lọc và xử lý đối tượng.
* **[mel/motionTrailToCurve.mel](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/mel/motionTrailToCurve.mel)**: Công cụ trail to curve.
* Các icon: `fake_const_icon.png` và `sets_icon.png`.

### 2. Khắc phục tương thích Python 2.7:
* Khử hoàn toàn lỗi cú pháp f-strings (`f"{...}"`) bằng cách viết lại theo kiểu định dạng chuỗi truyền thống `%` và `.format()` tại:
  * **[overlapper.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/overlapper.py)** (22 vị trí).
  * **[anm_hider.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/anm_hider.py)**.
* Đồng bộ tệp giao diện chính **[ui/window.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/ui/window.py)** từ bản Python 3 và nhúng module an toàn in ấn Unicode `__builtin__.print` ở đầu tệp để phòng tránh lỗi encode/decode khi hiển thị log trong Script Editor của Maya 2020.
* Khắc phục triệt để lỗi hiển thị ký tự có dấu (Mojibake - ví dụ `ÄÃ£ Ä‘Äƒng kÃ½...`) bằng cách bổ sung tiền tố Unicode `u` cho toàn bộ câu lệnh `print` có tiếng Việt trong:
  * **[menu.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/menu.py)**.
  * **[core/shelf.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/shelf.py)**.
  * **[core/selection_sets.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/selection_sets.py)**.
  * **[core/clean_redundant_keys.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/clean_redundant_keys.py)**.
  * **[core/arc_tracker.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/arc_tracker.py)**.
  * **[core/temp_pivot.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/temp_pivot.py)**.
  * **[ui/window.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/ui/window.py)**.
* Khử toàn bộ các chuỗi package name bị hardcode tĩnh như `"animeow_maya_toolboard_py2_v02"` hoặc `"animeow_maya_toolboard_v03"` trong các câu lệnh Mel/Python callback để tránh lỗi thực thi khi đổi tên package tại:
  * **[core/anm_hider.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/anm_hider.py)** (sử dụng biến `pkg_name` động).
  * **[ui/window.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/ui/window.py)** (nạp `pkg_name` trong `on_launch_anm_hider`).
* Khắc phục lỗi tạo Locator (`Object [None] is invalid`):
  * Nguyên nhân: Hàm `parent_to_animeow_group` trong `core/smart_link.py` không trả về tên mới của Locator sau khi đưa vào group, khiến biến `loc` trong `ui/window.py` bị gán bằng `None`. Khi gọi `cmds.select(created_locators)` sẽ báo lỗi do danh sách chứa `None`.
  * Giải pháp: Cập nhật hàm `parent_to_animeow_group` để trả về tên Locator/đường dẫn phân cấp mới (hoặc trả về chính nó nếu không reparent). Đồng thời thêm cơ chế kiểm tra giá trị trả về trước khi gán trong `ui/window.py` để đề phòng trường hợp lỗi.
* Khắc phục lỗi nạp module động (`AttributeError: 'NoneType' object has no attribute 'SelectionUtility'`):
  * Nguyên nhân: Khi người dùng reload tệp tin nhiều lần, nếu có một lần import bị lỗi Unicode (trước khi loại bỏ dấu), Python 2.7 sẽ cache trạng thái lỗi và gán thuộc tính `core.animeow_utilities = None` trên đối tượng module `core` cũ trong bộ nhớ. Sau khi reload, `core` không được làm sạch hoàn toàn các thuộc tính cũ, dẫn tới `animeow_utilities` vẫn trỏ đến `None`.
  * Giải pháp: Chuyển đổi cơ chế nạp toàn bộ submodules của `core` trong [window.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/ui/window.py) sang dạng **direct dynamic absolute import** (gọi trực tiếp `__import__(pkg_name + ".core.xxx")`). Phương pháp này bỏ qua việc truy cập gián tiếp qua `core.xxx`, giải quyết triệt để vấn đề cache `None` của cơ chế reload.
* Gỡ bỏ hoàn toàn công cụ Animo khỏi bộ công cụ Python 2 (`animeow_maya_toolboard_py2_v03`):
  * Lý do: Công cụ Animo không tương thích và không hoạt động ổn định trên môi trường Python 2 (Maya 2020) do chứa quá nhiều thành phần mã nguồn thiết kế riêng cho Python 3.
  * Các bước đã thực hiện:
    * Gỡ bỏ nút bấm **Animo** khỏi danh sách công cụ trong [shelf.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/shelf.py).
    * Gỡ bỏ hàm `launch_animo()` khởi chạy từ Shelf trong [shelf.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/core/shelf.py).
    * Gỡ bỏ nút bấm **Khoi dong Animo** và logic `on_launch_animo()` trong giao diện [window.py](file:///e:/AI_Work/Blender_Maya_Script/Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/ui/window.py).
    * Xóa bỏ hoàn toàn thư mục tài nguyên phụ trợ cồng kềnh `thirdparty/Animo_v5.9.6` trong gói Python 2 để giữ cho package nhẹ nhàng và sạch sẽ.

---

## Hướng dẫn Commit & Push Git

Chạy các lệnh sau tại thư mục dự án `e:\AI_Work\Blender_Maya_Script` để đưa code lên Git:

```bash
# 1. Thêm các thay đổi của thư mục toolboard Python 2 mới tạo và ghi chép nhật ký
git add Maya_script_2020_python_2/animeow_maya_toolboard_py2_v03/
git add takenote/2026-07-21.md

# 2. Commit thay đổi
git commit -m "feat(py2_v03): upgrade animeow maya toolboard py2 to v03 with py2.7 compatibility and new features"

# 3. Push lên repository từ xa
git push
```
