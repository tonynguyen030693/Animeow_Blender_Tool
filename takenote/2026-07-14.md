# Take Note - 2026-07-14

Hôm nay đã hoàn tất việc nâng cấp Phase 3 UI/UX, tối ưu hóa hiệu năng hệ thống Link/Bake, sửa toàn bộ lỗi cài đặt/khởi chạy, gom nhóm quản lý locator tạm thời dưới group `Animeow_locator` và đồng bộ tiền tố `Anm_loc_` cho **Animeow Maya Toolboard v02** (Python 3):

## 1. Hoàn Thành Phase 3 UI/UX Overhaul ( window.py )
* **Vá lỗi crash Tween Machine**: Bổ sung 2 handler bị khuyết thiếu `on_tween_apply` và `on_tween_preset` để xử lý sự kiện kéo slider và click preset %.
* **Gắn icon Vector QPainter**: Nạp icon phẳng monochrome Cyan từ bộ `AnimeowIcons` vào hơn 40 nút bấm chức năng trên cả 5 Tab chính và cửa sổ Favorites standalone.
* **Tối ưu Spacing**: Co nhỏ `setContentsMargins` xuống `(6, 8, 6, 6)` và `setSpacing` xuống `8` giúp bố cục compact và chuyên nghiệp hơn.

## 2. Tối Ưu Hóa Hiệu Năng Link & Bake ( smart_link.py )
* **Tạo Link siêu tốc (x10 - x20)**: Loại bỏ hoàn toàn vòng lặp Python duyệt `currentTime` và `matchTransform` thủ công. Thay bằng tạo parent/scale constraint tạm thời và gọi `cmds.bakeResults` chạy ngầm ở mức core C++ của Maya. Giúp viewport không còn bị giật chớp màn hình.
* **Bake & Clean hàng loạt (x10 - x50)**: Gom các keyframe thừa cần xóa và thực thi hàng loạt bằng `cmds.selectKey` + 1 lệnh `cmds.cutKey` duy nhất, loại bỏ việc gọi `cutKey` liên tiếp trong vòng lặp gây nghẽn DG evaluation.

## 3. Khắc Phục Lỗi Khởi Chạy Từ Shelf & Cài Đặt ( install.mel & shelf.py )
* **Sửa lỗi Drag-and-Drop**: Cập nhật file `install.mel` để nhận diện chính xác thư mục cha và import đúng package `animeow_maya_toolboard_v02`.
* **Tự động chèn Path cho các nút trên Shelf**: Cập nhật hàm `create_shelf` trong `shelf.py` để tự động tính toán thư mục dự án hiện hành và chèn lệnh chèn path (`sys.path.insert`) động vào command của **toàn bộ 20 nút trên Shelf**. Giúp loại bỏ triệt để lỗi `ModuleNotFoundError` khi animator click vào nút công cụ.

## 4. Quản Lý & Tự Động Dọn Dẹp Locator Tạm ( smart_link.py, world_bake.py, temp_pivot.py, space_order_tool.py )
* **Đồng bộ tiền tố `Anm_loc_`**: Đổi tên toàn bộ locator tạm thời do tool sinh ra sang tiền tố thống nhất (ví dụ: `Anm_loc_link_parent_`, `Anm_loc_bake_`, `Anm_loc_pivot_`, `Anm_loc_rotOrder_`) giúp animator dễ nhận biết trong Outliner.
* **Gom nhóm dưới `Animeow_locator`**: Tự động tạo và parent mọi locator do Animeow Tool sinh ra vào một group cha duy nhất tên là `Animeow_locator` để Outliner luôn ngăn nắp.
* **Cơ chế tự hủy (Auto-cleanup)**: Khi nướng trả chuyển động (Bake) hoặc giải phóng pivot/order, các locator bị xóa đi. Nếu group `Animeow_locator` trống rỗng, hệ thống sẽ tự động xóa sạch luôn cả group cha này.

---
* Tất cả thay đổi chỉ tác động trong phạm vi folder `Maya_script_2022_python_3/animeow_maya_toolboard_v02` đúng theo yêu cầu.
